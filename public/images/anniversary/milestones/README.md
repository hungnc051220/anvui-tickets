These nine 80 × 80 milestone thumbnails were cropped from the user-supplied
`DESIGN_REFERENCE.png` mockup for visual fidelity. They are temporary:
replace them with the original AN VUI photographs when those files are
available. The 2026 milestone uses the existing `/assets/logo2.png`.
